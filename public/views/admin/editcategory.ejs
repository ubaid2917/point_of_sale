<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Category</title>
  <style>
    body {
      margin: 0;
      padding: 0;
      font-family: Arial, sans-serif;
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
      background-color: rgb(198, 197, 197);
    }

    .login-container {
      background-color: #ffffff;
      border-radius: 10px;
      box-shadow: 0 10px 20px rgba(0, 0, 0, 0.2);
      padding: 40px;
      width: 400px;
      text-align: center;
      animation: fadeIn 0.6s ease-in-out;
    }

    @keyframes fadeIn {
      from {
        opacity: 0;
      }
      to {
        opacity: 1;
      }
    }
      .input-group select {
      width: 100%;
      padding: 12px;
      border: none;
      border-radius: 5px;
      font-size: 16px;
      background-color: #f7f7f7;
      color: #333333;
      transition: background-color 0.3s;
    }

    .input-group select:focus {
      background-color: #ffffff;
      outline: none;
    }

    .login-container h2 {
      margin-bottom: 20px;
      color: #333333;
      font-size: 28px;
    }

    .input-group {
      margin-bottom: 20px;
    }

    .input-group label {
      display: block;
      font-size: 16px;
      color: #333333;
      margin-bottom: 8px;
      text-align: left;
    }

    .input-group input {
      width: 100%;
      padding: 12px;
      border: none;
      border-radius: 5px;
      font-size: 16px;
      background-color: #f7f7f7;
      color: #333333;
      transition: background-color 0.3s;
    }

    .input-group input:focus {
      background-color: #ffffff;
      outline: none;
    }

    .submit-button {
      background-color: #3498db;
      color: #ffffff;
      border: none;
      border-radius: 5px;
      padding: 14px 24px;
      font-size: 18px;
      cursor: pointer;
      transition: background-color 0.3s, transform 0.2s;
      overflow: hidden;
      position: relative;
    }

    .submit-button:hover {
      background-color: #2980b9;
      transform: translateY(-2px);
    }

    .submit-button::before {
      content: "";
      position: absolute;
      width: 100%;
      height: 100%;
      background-color: rgba(255, 255, 255, 0.1);
      top: 0;
      left: -100%;
      transition: left 0.3s;
    }

    .submit-button:hover::before {
      left: 100%;
    }
  </style>
</head>
<body>
  <div class="login-container">
   
    <h2>Edit Category</h2>
    
    <form action="/updatecategory/<%= category._id %>" method="POST">
      
      <div class="input-group">
        <label for="category">Category</label>
        <input type="category" id="category" name="name" value="<%= category.name %>"  placeholder="Enter category name">
      </div>
     
      
      <button class="submit-button" type="submit">Submit</button>
    </form>
  </div>
</body>
</html>
